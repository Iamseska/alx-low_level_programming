#include "main.h"ing changes.  Quit, or continue with caution.
#include "unistd.h" for this file crashed.

/** 
 * _putchar - writes the character c to stdouty").
 * @c: The character to print
 *  
 * Return: on success 1.
 * on error, -1 is returned, and errno is set appropriatelty.
 */
int _putchar(char c)
{
        return (write(1, &c, 1));
}
